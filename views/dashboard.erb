<h1>ダッシュボード</h1>
<p><a href="/draw">お絵かきする</a></p>
<% if adult == 'true' %>
  <p><a href="/dashboard">エロなし</a></p>
<% else %>
  <p><a href="/dashboard?adult=true">エロあり</a></p>
<% end %>

<% posts.each do |post| %>
  <div class="post">
    <h2>
      <%= post["title"] %>
    </h2>
    <p><%= post["eroi"] %></p>
    <p class="post-image">
    <img src="/uploads/<%= post["src"] %>" alt="<%= post["title"] %>">
    </p>
    <p>
    💖
    <button class="like" data-id="<%= post["id"] %>">
      <%= post["likes"] %>
    </button>
    </p>
  </div>
<% end %>
<script src="/js/dashboard.js"></script>
